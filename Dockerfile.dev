FROM node:20-alpine

WORKDIR /app

COPY package*.json ./

RUN npm install

COPY . .

RUN mkdir -p public
RUN cp node_modules/pdfjs-dist/build/pdf.worker.mjs public/pdf.worker.mjs 2>/dev/null || echo "Will copy worker manually"

EXPOSE 5174

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
